GIT 


git je memory card pro kod 

git init -> pouští git 

git add . -> všchno uloží 

git commit -> ukalda zmeny do pameti na tvrdo 
    git commit -m "komentar co jsem zmenil" 


git log -> ukaže všechny změny 



new branch -> ta ktera je moje 

pushuju do githubu 
puluju z githubu ke mě 


novej branch se děla: git checkout -b novejbranch 

jak pushovat do gitu 
- git init
- git add . 
- git commit -m "zprava" 
- git remote add origin URl 
- git push origin master 
        - master je ten branch 


Git history 
- commit je změna co jsem udělal 
    je to takovy checkpoint 

- commity se pushuji nahoru 

bRNACH 
- comity jsou proste savy 
- master je vždy hlavní branch - vzdy ten spraávny  


- novej branch -> je kopie master s novou vlastností 
    - vhodné pro pushovaní nových features bez ovlivnění master branche 

    - pokud je vše hotové, mergne se s master, tak přesně aby vše fungovalo 

    - stále mohou do nové branche pullovat nové věci z master 
    - také mohu branchovat z jiné vedlejší branche 


vždy musím dat změny do stage -> git add 

změna brancha: git checkout master 


mergovaní: git marge master 

u pushovaní 
    -u origin master 
        funguje tak, že pro příší push stačí napsat 
            git push 